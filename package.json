{
  "name": "apollo-study",
  "version": "1.0.0",
  "description": "Apollo Study",
  "scripts": {
    "start": "node dist/index.js",
    "build": "parcel build src/index.ts --target node",
    "dev": "nodemon --exec 'yarn build && yarn start' -e ts --watch src"
  },
  "author": "uihyeon.cha",
  "license": "ISC",
  "dependencies": {
    "apollo-server": "^2.19.2",
    "graphql": "^14.7.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.7",
    "parcel-bundler": "^1.12.4",
    "typescript": "^4.1.3"
  }
}
